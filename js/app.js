(function(){
 
  /*
     * Declaration of main angular module for this application.
     *
     * It is named FactsandQuiz and has no dependencies (hence the 
     * empty array as the second argument)
     */
    angular
        .module("FactsandQuiz", [ ]);
 
})();